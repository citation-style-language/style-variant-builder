[project]
name = "csl-builder"
version = "0.1.0"
description = "Create variant Citation Style Language (CSL) styles"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["lxml>=5.3.1", "lxml-stubs>=0.5.1"]
